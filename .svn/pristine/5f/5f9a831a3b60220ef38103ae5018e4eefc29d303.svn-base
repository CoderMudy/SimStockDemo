//
//  UserConst.h
//  SimuStock
//
//  Created by Mac on 15/4/3.
//  Copyright (c) 2015年 Mac. All rights reserved.
//

#import <Foundation/Foundation.h>

/** 用户Vip项类型*/
typedef NS_ENUM(NSUInteger, UserVipType) {
  /** 未开通Vip */
  NotVipType = -1,
  
  /** Vip用户 */
  VipUser = 1,
  
  /** SVip用户 */
  SVipUser = 2,
  
  /** 过期的Vip用户 */
  VipUserExpired = 3
};

/** 用户类型 */
typedef NS_ENUM(NSUInteger, UserBindType) {

  /** QQ注册账号（不可解绑） */
  UserBindTypeQQ = 2,
  
  /** 新浪微博注册账号（不可解绑） */
  UserBindTypeSinaWeibo = 3,
  
  /** 手机号注册账号（不可解绑） */
  UserBindTypePhoneRegister = 4,
  
  /** 微信注册账号（不可解绑） */
  UserBindTypeWeixinRegister = 7,
  
  /** 手机号绑定已有账号(可更改，不可解绑) */
  UserBindTypeBindPhone2Exist = 1,
  
  /** QQ绑定已有账号(可解绑) */
  UserBindTypeBindQQ2Exist = 5,
  
  /** 新浪绑定已有账号(可解绑) */
  UserBindTypeBindSinaWeibo2Exist = 6,
  
  /** 微信绑定已有账号(可解绑) */
  UserBindTypeBindWeixin2Exist = 8,
};


/** 用户类型 */
typedef NS_ENUM(NSUInteger, UserLoginType) {
  /** 手机号登录 */
  UserLoginTypePhone = 1,
  
  /** QQ登录 */
  UserLoginTypeQQ = 2,
  
  /** 新浪微博登录 */
  UserLoginTypeSinaWeibo = 3,
  
  /** 微信登录 */
  UserLoginTypeWeixin = 4,
};






@interface UserConst : NSObject

@end
